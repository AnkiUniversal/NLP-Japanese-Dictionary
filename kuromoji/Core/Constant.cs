
namespace NLPJDict.Kuromoji.Core
{
    public static class Constant
    {
        public const string TOKEN_NOT_HAVE = "*";

        public const string RESOURCE_PATH = @"./kuromoji/";

        public const int INTEGER_BYTES = sizeof(int) / sizeof(byte);

        public const int SHORT_BYTES = sizeof(short) / sizeof(byte);

        public const int CHAR_SIZE = sizeof(char)*8;
    }
}
